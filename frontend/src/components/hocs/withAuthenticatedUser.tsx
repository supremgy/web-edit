import styled from 'styled-components';

const withAuthenticatedUser = () => {
  return (
    <div>
      <h1>withAuthenticatedUser</h1>
    </div>
  );
};

const withAuthenticatedUserStyle = styled.div``;

export default withAuthenticatedUser;
